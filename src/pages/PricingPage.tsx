import { Pricing } from "@/components/Pricing";

/** Standalone pricing page — same UI as the pricing section on the landing page. */
export function PricingPage() {
  return (
    <main>
      <Pricing />
    </main>
  );
}
